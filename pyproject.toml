[project]
name = "cmdi-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cachetools>=6.2.0",
    "fastapi>=0.116.1",
    "gunicorn>=23.0.0",
    "httpx>=0.28.1",
    "redis>=6.4.0",
    "uvicorn>=0.35.0",
    "saxonche>=12.9.0",
]

[tool.uv]
environments = [
    "sys_platform == 'linux' and platform_machine == 'aarch_64'"
]
