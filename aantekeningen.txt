0.522 hint: You're on Linux (`linux_x86_64`), but `saxonche` (v12.9.0) only has wheels for the following platforms: `manylinux_2_24_aarch64`, `manylinux_2_24_x86_64`, `macosx_10_11_x86_64`, `macosx_11_0_arm64`, `win_amd64`; consider adding "sys_platform == 'linux' and platform_machine == 'x86_64'" to `tool.uv.required-environments` to ensure uv resolves to a version with compatible wheels


[project]
name = "cmdi-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cachetools>=6.2.0",
    "fastapi>=0.116.1",
    "gunicorn>=23.0.0",
    "httpx>=0.28.1",
    "redis>=6.4.0",
    "uvicorn>=0.35.0",
    "saxonche>=11.7.0", 
]
[tool.uv]
environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'"
]


